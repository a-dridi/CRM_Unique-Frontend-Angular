import { AgoDateStringFormatPipe } from './ago-date-string-format.pipe';

describe('AgoDateStringFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AgoDateStringFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
